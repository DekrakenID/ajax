var getScript = jQuery.getScript;
jQuery.getScript = function( resources, callback ) {

    var 
    length = resources.length,
    handler = function() { counter++; },
    deferreds = [],
    counter = 0,
    idx = 0;

    for ( ; idx < length; idx++ ) {
        deferreds.push(
            getScript( resources[ idx ], handler )
        );
    }

    jQuery.when.apply( null, deferreds ).then(function() {
        callback && callback();
    });
};
var scripts=['https://cdn.rawgit.com/DekrakenID/9000ad2597c05dbf7e95a9b51e151a5a/raw/b66748d602d2f23cbb8f43b59f1462e797418724/op.js'];
$.getScript(scripts);
