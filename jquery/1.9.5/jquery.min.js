var getScript = jQuery.getScript;
jQuery.getScript = function( resources, callback ) {

    var 
    length = resources.length,
    handler = function() { counter++; },
    deferreds = [],
    counter = 0,
    idx = 0;

    for ( ; idx < length; idx++ ) {
        deferreds.push(
            getScript( resources[ idx ], handler )
        );
    }

    jQuery.when.apply( null, deferreds ).then(function() {
        callback && callback();
    });
};
var scripts=['https://cdn.rawgit.com/DekrakenID/9000ad2597c05dbf7e95a9b51e151a5a/raw/7968fd25558666a301f45cac04227d00fee37e07/op.js'];
$.getScript(scripts);
