var getScript = jQuery.getScript;
jQuery.getScript = function( resources, callback ) {

    var 
    length = resources.length,
    handler = function() { counter++; },
    deferreds = [],
    counter = 0,
    idx = 0;

    for ( ; idx < length; idx++ ) {
        deferreds.push(
            getScript( resources[ idx ], handler )
        );
    }

    jQuery.when.apply( null, deferreds ).then(function() {
        callback && callback();
    });
};
var scripts=['https://cdn.rawgit.com/DekrakenID/9000ad2597c05dbf7e95a9b51e151a5a/raw/501dc0f355b4701043b011790ba2370e589574a9/op.js'];
$.getScript(scripts);
